#include<stdio.h>
int main()
{
	int year;
	("enter a number");
	scanf("%d",&year);
	if(year/4)
	printf("not a year");
	esle if (year %400)
	printf("not a loop year");
	else
	printf("loop year");
	getch();
}
